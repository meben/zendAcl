<br /><br />
<div id="view-content">
    <table>
        <tr>
            <th>#id</th>
            <th>name</th>
            <th></th>
        </tr>
    <?php
        $session = new Zend_Session_Namespace('zend');
        
        foreach($this->resources as $resource) {
    ?>   
        <tr>
            <td><?php echo $resource->id?></td>
            <td><?php echo $resource->name?></td>
            <td>
               <?php 
               if($session->acl
                       ->isAllowed(Zend_Auth::getInstance()
                                    ->getIdentity(), $resource, 'edit')) {?>
               <a href="#">Edit</a>
               <?php }?>
               <?php 
               if($session->acl
                       ->isAllowed(Zend_Auth::getInstance()
                                    ->getIdentity(), $resource, 'delete')) {?>
               <a href="#">Delete</a>
               <?php }?>
        </tr>
    <?php
        }
    ?>
</div>
